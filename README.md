This project is designed to launch as:
Standalone app in development mode via "npm run dev"
Full production app via "docker compose up -d --build"

Requirments:
Node.js ^v22.x.x
Docker (required for production mode only)